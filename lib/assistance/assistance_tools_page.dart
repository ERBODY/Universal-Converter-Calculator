import 'package:flutter/material.dart';

class AssistanceToolsPage extends StatelessWidget {
  const AssistanceToolsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Assistance Tools'),
      ),
      body: const Center(
        child: Text('Assistance Tools Page'),
      ),
    );
  }
}
